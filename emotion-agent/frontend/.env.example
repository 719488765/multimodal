# 留空：通过 npm run dev 同源访问，由 Vite 将 /api 代理到 127.0.0.1:8000（服务器本机浏览器推荐）
# VITE_API_BASE_URL=

# 【Cursor / SSH 远程开发】在 Windows 浏览器访问时，若「健康检查正常但推理 Failed to fetch」：
# 1. 在 Cursor 里再转发远程 8000 → 本地 8000
# 2. 取消下行注释，重启 npm run dev
# VITE_API_BASE_URL=http://127.0.0.1:8000

# 静态部署到 nginx 时：
# VITE_API_BASE_URL=http://<服务器IP>:8000
