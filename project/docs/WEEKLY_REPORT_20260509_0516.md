# 工作总结周报（准确率序列 AP0–AP4 + 论文材料）

**姓名**：李智春  
**周期**：2026年5月9日–2026年5月16日（承接 [`WEEKLY_REPORT_20260425_0508.md`](WEEKLY_REPORT_20260425_0508.md) 截至 **2026-05-08** 之后的进展；本周报截至 **2026-05-16**）  
**关联文档**：[`EXPERIMENT_ACCURACY_SEQUENCE_GUIDE.md`](EXPERIMENT_ACCURACY_SEQUENCE_GUIDE.md)、[`EXPERIMENT_ACCURACY_SEQ_MAIN_RECORD.md`](EXPERIMENT_ACCURACY_SEQ_MAIN_RECORD.md)、[`THESIS_EXPERIMENT_MASTER_SUMMARY.md`](THESIS_EXPERIMENT_MASTER_SUMMARY.md)、[`THESIS_FULL_DRAFT_MULTIMODAL_EMOTION.md`](THESIS_FULL_DRAFT_MULTIMODAL_EMOTION.md)

---

## 一、本周工作总览（结论先行）

本周主线为 **AP4（域适应扫描）** 从「单点启动」推进到 **2 组满 50 epoch + 1 组续训中**，并落地 **串行自动化流水线** 与 **训练稳定性修复**（解决 w005 在 epoch 28 因 DataLoader worker 段错误崩溃的问题）。

1. **AP4 实验**：完成 **DA 默认（①）**、**domain_loss_weight=0.02（②）** 共 2 组满程；**w=0.05（③）** 训练至 epoch 27 后于 5/15 崩溃，5/16 已从 checkpoint 续训并接入后续 4 组串行队列。  
2. **工程运维**：新增 `scripts/run_ap4_rest_serial_continue.sh`，在 tmux 会话 **`aseq_ap4_rest_serial`** 中执行「w005 续训 → w010 → w05lr → uniform → seed3407」；修复 DataLoader / 视频加载 / HF 离线 / 续训优化器兼容等问题。  
3. **AP0–AP3**：本周 **无新增训练**；**AP3 `two_stage` 仍仅 28/50 epoch**，与 5/8 周报状态一致，定稿前仍须续训。  
4. **论文材料**：AP4 ①② 已具备可写入主表的 **Best/Last 双指标**；全表结论须待 AP4 七组全部完成后再冻结。

**当前总体状态**：**AP0–AP3 主表可支撑论文初稿**；**AP4 完成度 2/7（满程）+ 1/7（续训中）+ 4/7（排队）**；智能体 checkpoint 绑定建议待 AP4 扫完后选定「主发布线」。

---

## 二、实验线进展：本周增量（相对 2026-05-08）

> **数据真源**：`project/logs_accuracy_seq/<run>/metrics.csv` 中 `phase=val` 行；快照时间 **2026-05-16**。定稿前建议对论文主 run 使用 `scripts/recompute_val_metrics.py` 交叉验证。

### 2.1 本阶段执行清单

| 阶段 | 本周主要动作 | 状态（截至 2026-05-16） |
|------|----------------|-------------------------|
| AP0 | — | **已完成**（50 epoch，本阶段未改） |
| AP1 | — | **已完成**（6 组单域，本阶段未改） |
| AP2 | — | **已完成**（6 组配方消融，本阶段未改） |
| AP3 | — | **基本完成**；`two_stage` **仍 28/50 epoch**（本阶段未续训） |
| AP4 ① DA 默认 | `AP4_AVT_pretrain_3datasets_DA_20260508_200639` | **已完成**（5/8 启动，约 5/12 满程） |
| AP4 ② w=0.02 | `AP4_AVT_pretrain_3datasets_DA_w002_20260511_193213` | **已完成**（5/11 起，50 epoch） |
| AP4 ③ w=0.05 | `AP4_AVT_pretrain_3datasets_DA_w005_20260514_071550` | **续训中**（5/14 开跑；5/15 ep28 崩溃；5/16 从 ep24 ckpt 续训） |
| AP4 ④–⑦ | w010 / w05lr / uniform / seed3407 | **未启动**（已写入串行脚本队列） |

---

### 2.2 AP4（域适应扫描，本周核心）

| 序号 | Run 目录（缩写） | n_val | Last Acc / F1 @ep | Best Acc @ep | Best F1 @ep | 本周状态 |
|------|------------------|-------|-------------------|--------------|-------------|----------|
| ① | `..._DA_20260508_200639` | 50 | 0.5479 / 0.4962 @49 | **0.5504 @45** | **0.4983 @45** | **满程完成** |
| ② | `..._DA_w002_20260511_193213` | 50 | 0.5673 / 0.5091 @49 | **0.5710 @47** | **0.5184 @22** | **满程完成** |
| ③ | `..._DA_w005_20260514_071550` | 28 | 0.5616 / 0.5104 @27 | 0.5646 @26 | 0.5124 @25 | **中断后续训**（续训从 ep25 起） |
| ④ | `ap4_config_AVT_DA_w010_accuracy_seq.yaml` | — | — | — | — | 排队 |
| ⑤ | `ap4_config_AVT_DA_w005_lr5e5_accuracy_seq.yaml` | — | — | — | — | 排队 |
| ⑥ | `ap4_config_AVT_DA_uniform_accuracy_seq.yaml` | — | — | — | — | 排队 |
| ⑦ | `ap4_config_AVT_DA_seed3407_accuracy_seq.yaml` | — | — | — | — | 排队 |

**评价与消融要点**：

1. **w002 峰值优于 DA 默认**（Best Acc 0.5710 vs 0.5504），与主线重跑「DA 峰值可能优于 noDA」的讨论方向一致；论文须 **Best 与 Last 同表**，并写明 `domain_loss_weight` 与 **checkpoint 选点（建议对齐 `checkpoint_pretrain_best_f1.pth`）**。  
2. **w005 中断前指标与 w002 同量级**，崩溃属 **工程问题**（DataLoader worker segfault），非配方失效；续训因骨干冻结策略更新导致 **优化器状态未加载**，将重跑约 ep25–27，以 **best_f1** 为论文选点依据即可。  
3. **相对 AP2 峰值**（如 M1 Best Acc ≈ 0.6068）：AP4 为 **standard 融合 + DA**，与 AP2 **emotion_shift + 无 DA** 非同一协议，**禁止混表排名**；应在论文中分开展示并脚注 yaml 差异（见指南 **§5.3**）。

---

### 2.3 AP0–AP3（本周无新增 run，供对照引用）

AP0–AP2、AP3 中 standard / leader_text / leader_audio 的满程数值仍以 [`WEEKLY_REPORT_20260425_0508.md`](WEEKLY_REPORT_20260425_0508.md) **§2.2–§2.5** 与 [`THESIS_EXPERIMENT_MASTER_SUMMARY.md`](THESIS_EXPERIMENT_MASTER_SUMMARY.md) **§6.2** 为准。本周需强调的遗留项：

| 项 | 状态 | 说明 |
|----|------|------|
| AP3 `two_stage` | n_val=28 | `AP3_fusion_two_stage_3ds_s3407_20260501_110720`，**未满 50 epoch** |
| AP2 混合峰值参考 | Best Acc ≈ 0.61 | M1/M2，作为 AP4 对照上界时需分表 |

---

## 三、工程与运维（本周完成）

### 3.1 AP4 串行流水线

| 组件 | 路径 / 会话 | 说明 |
|------|-------------|------|
| 串行脚本 | `scripts/run_ap4_rest_serial_continue.sh` | w005 `--resume` → 依次 w010、w05lr、uniform、seed3407 |
| tmux 会话 | `aseq_ap4_rest_serial` | `GPU_ID=0`；驱动日志 `logs_accuracy_seq/ap4_rest_serial_driver.log` |
| 续训日志复用 | 环境变量 `MULTIMODAL_LOG_RUN_DIR_NAME` | w005 续写原目录 `..._w005_20260514_071550` |

**禁止操作**：勿使用 `./scripts/start_accuracy_seq_tmux.sh ap4` 或 `start_accuracy_seq_gpuaware.sh ap4` 一次拉起 7 路（极易 OOM）；须遵守指南 **§4.4**「每卡同时一路 AVT+DA」。

### 3.2 训练稳定性修复（针对 2026-05-15 w005 @epoch28 崩溃）

| 问题 | 处理 |
|------|------|
| DataLoader worker 段错误 | `utils/helpers.py`：`dataloader_worker_init_fn`、`get_dataloader_kwargs`；`train.py` 统一接入 |
| 视频整片解码占满 worker | `data/dataset.py`：按帧索引采样 + `finally` 释放 `VideoCapture` |
| HF `hf-mirror.com` DNS 失败 | 串行脚本内 `unset HF_ENDPOINT` + `HF_HUB_OFFLINE=1` |
| BERT/Wav2Vec2 checkpoint 警告 | `feature_extractors.py`：冻结骨干并关闭 gradient checkpointing |
| 续训优化器参数组不匹配 | `train.py`：加载失败时跳过 optimizer/scheduler 并打 WARNING |
| w005 配置 | `ap4_config_AVT_DA_w005_accuracy_seq.yaml`：`num_workers: 4 → 2` |

### 3.3 运维命令（备忘）

```bash
tmux attach -t aseq_ap4_rest_serial
tail -f project/logs_accuracy_seq/ap4_rest_serial_driver.log
tail -f project/logs_accuracy_seq/AP4_AVT_pretrain_3datasets_DA_w005_20260514_071550/metrics.csv
./scripts/tensorboard_accuracy_seq.sh 6006
```

---

## 四、实验评价、风险与论文写法（压缩版）

### 4.1 本周可写入论文的增量

- **表：AP4 域损失扫描（部分）**：①② 两行 Best/Last；③ 标注「进行中/续训」脚注。  
- **讨论：训练可信度**：w005 崩溃与恢复可作为「长程混合训练需工程加固」短例（若方法节有 reproducibility 小节）。  
- **DA 有效性**：暂不可写「最优 w=」结论；待 ④–⑦ 完成后做 **w002 / w005 / w010** 敏感性曲线。

### 4.2 风险登记

| 风险 | 现象 | 缓解 |
|------|------|------|
| DataLoader 再崩 | worker segfault | 已降 `num_workers`；必要时临时 `num_workers: 0` |
| 同卡 OOM | 多路 AVT 并行 | 坚持串行脚本；`nvidia-smi` 开跑前自检 |
| 续训优化器重置 | 与 ep27 前曲线不完全连续 | 论文以 **best_f1 checkpoint** 为准；方法节一句说明 |
| 文档滞后 | `EXPERIMENT_ACCURACY_SEQ_MAIN_RECORD.md` §7 AP4 仍占位 | AP4 七组完成后一次性刷新 |
| AP3 缺口 | `two_stage` 未满程 | P1 单独 tmux 续训，勿与 AP4 抢同卡 |

---

## 五、本周时间线（便于汇报）

| 日期 | 事项 |
|------|------|
| 05-08–05-12 | AP4 ① DA 默认训练推进并完成（50 epoch） |
| 05-11 起 | AP4 ② w002 开跑并完成（50 epoch） |
| 05-14 | AP4 ③ w005 开跑（tmux `aseq_ap4_rest_serial`） |
| 05-15 | w005 训练至 epoch 28 时 DataLoader worker 段错误，进程退出 |
| 05-16 | 工程修复；`run_ap4_rest_serial_continue.sh` 启动 w005 续训（自 `checkpoint_pretrain_epoch_24.pth`，ep25 起）并排队 ④–⑦ |

---

## 六、下一阶段工作计划

### P0（必须完成，GPU 连续占用约 4–6 天）

1. **续完 w005 至 50 epoch**：保持 `aseq_ap4_rest_serial`，勿同卡叠第二路大训练。  
2. **串行完成 AP4 ④–⑦**：依赖脚本自动衔接，无需再手动 `tmux new-session` 七路。  
3. **刷新主记录**：更新 [`EXPERIMENT_ACCURACY_SEQ_MAIN_RECORD.md`](EXPERIMENT_ACCURACY_SEQ_MAIN_RECORD.md) **§7.2**、[`THESIS_EXPERIMENT_MASTER_SUMMARY.md`](THESIS_EXPERIMENT_MASTER_SUMMARY.md) **§6.2** AP4 全表。  
4. **TensorBoard 证据包**：导出 AP0→AP4 主 run 的 `val/accuracy`、`val/f1`、`val/cls_ce_unweighted` 至 `project/docs/figures/tensorboard/`。

### P1（可与 P0 并行，不占 GPU 或另卡）

1. **AP3 two_stage 续训**：`--resume` + `MULTIMODAL_LOG_RUN_DIR_NAME=AP3_fusion_two_stage_3ds_s3407_20260501_110720`。  
2. **数据健康检查**（可选）：`scripts/check_media_health.py`；已知坏样本 `meld_train_1166`（`data/bad_samples_train_dir.csv`）。  
3. **数值交叉验证**：`scripts/recompute_val_metrics.py` 核对 AP4 ①② 的 Best F1 与 CSV 一致。  
4. **同步论文草稿**：[`THESIS_FULL_DRAFT_MULTIMODAL_EMOTION.md`](THESIS_FULL_DRAFT_MULTIMODAL_EMOTION.md) 第 4 章以主记录 §6 为真源替换「待回填」。

### P2（AP4 全部完成后）

1. 撰写 AP4 **Best vs Last** 与 **domain_loss_weight** 敏感性小节。  
2. 确定智能体 **主发布线** checkpoint（主文档 **§12**：峰值 vs 稳定性权衡）。  
3. （可选）AP5 CREMA 微调、AP2opt 子阶段、emotion-agent 端到端延迟测试。

### 时间粗算（单卡串行）

- 单 run：约 35–45 min/epoch × 50 epoch ≈ 29–38 h。  
- 本周剩余：w005 约 25 epoch + 4 组全新 ≈ **4–5 天**（视 GPU 负载与是否中断而定）。

---

## 七、参考文献（文档内）

- [`EXPERIMENT_ACCURACY_SEQUENCE_GUIDE.md`](EXPERIMENT_ACCURACY_SEQUENCE_GUIDE.md)：AP4 分会话启动 **§4.4**、日志隔离、评价口径 **§5**。  
- [`config/rerun/accuracy_plan/ap4_da_sweep_manifest.yaml`](../config/rerun/accuracy_plan/ap4_da_sweep_manifest.yaml)：AP4 七组配置清单。  
- [`EXPERIMENT_RERUN_FULL_RECORD_20260407.md`](EXPERIMENT_RERUN_FULL_RECORD_20260407.md)：历史重跑锚点（勿与 `logs_accuracy_seq` 混用）。  
- [`WEEKLY_REPORT_20260425_0508.md`](WEEKLY_REPORT_20260425_0508.md)：上一周期周报。

---

**文档版本**：v1.0  
**数据快照**：`logs_accuracy_seq/AP4_*/*/metrics.csv`（截至 **2026-05-16**；w005 续训进行中，表中 n_val=28 为中断前快照）  
**备注**：体例对齐 [`WEEKLY_REPORT_20260425_0508.md`](WEEKLY_REPORT_20260425_0508.md)；AP4 满程后请在本文件 **修订记录** 追加一行并更新 §2.2 表格。

### 修订记录

| 日期 | 摘要 |
|------|------|
| 2026-05-16 | 初版：汇总 5/9–5/16 AP4 ②组满程、w005 崩溃与续训、串行脚本与工程修复、下周 P0–P2 计划。 |
